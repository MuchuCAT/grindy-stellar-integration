{
  "name": "@grindy/soroswap-adapter",
  "version": "0.1.0",
  "description": "Soroswap Router event adapter for normalized Grindy campaign events.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check-types": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@grindy/adapter-sdk": "workspace:*",
    "@grindy/campaign-event-schema": "workspace:*",
    "@stellar/stellar-sdk": "^15.1.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
